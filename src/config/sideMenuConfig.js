import pathConfig from 'config/pathConfig';

const SideMenuConfig = [
  {
    name: pathConfig.home.name,
    path: pathConfig.home.path,
    id: pathConfig.home.path,
    icon: pathConfig.home.icon,
    // children: [],
  },

];

export default SideMenuConfig;
